//FUNCTIONS - can be written as declarations or expressions.
/*Types of functions:
    -Function Delcartion

    function delcation(){
        console.log("This is where the code is executed inside the function body")
    }

    //These things are hoisted and available anywhere in the program.

    -Function Expression

    //These things are not hoisted after creation... stored within a variable instead.

    let expression = funtion(){ //<-- an anonymous funtion
        console.log("This is a function expresion") 
    }
    -Anonymous Functions

*/
